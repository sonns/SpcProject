[Users]
* = admin ; Only user role can access index action

[Departments]
index, view = user ; Only user role can access index and view actions
* = admin ; Only admin role can access index and view actions

[Requests]
* = admin ; Editor and admin roles have access to all actions
